Recreate the image. Create a HTML-file with the appropriate html5 tags. No CSS is needed for this exercise. The head should contain a title and the body should contain a h1-tag. At the end of the body, include a script that links to a new file called main.js. In the main.js file write the code that shows an alertbox containing the text “IT works!”. 